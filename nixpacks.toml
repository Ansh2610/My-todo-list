[phases.setup]
nixPkgs = ["python310", "gcc", "libGL"]

[phases.install]
cmds = ["pip install -r backend/requirements.txt", "python -c 'from ultralytics import YOLO; YOLO(\"yolov8n.pt\")'"]

[phases.build]
cmds = []

[start]
cmd = "cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
